apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sifnode.fullname" . }}-test-connection"
  labels:
    {{- include "sifnode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sifnode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
