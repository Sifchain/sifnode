name: Node.js CI

on:
  push:
    branches: [develop, master]
    paths: ['ui/**']
  pull_request:
    branches: [develop, master, frontend-ui] # Temporarily add frontend-ui until we merge it
    paths: ['ui/**']

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui

    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: ^1.15
        id: go
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2.1.2
        with:
          node-version: '14.x'
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - name: Cache Node.js modules
        id: yarn-cache
        uses: actions/cache@v1
        with:
          path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: ${{ runner.os }}-yarn-
      - name: Install dependencies
        run: yarn install --frozen-lockfile --silent && cd ./chains/ethereum && yarn install --frozen-lockfile --silent
        env:
          CI: true
      - name: Build test chains
        run: yarn chain:build
      - name: Test Frontend
        run: yarn test
        env:
          CI: 1