import { Context } from ".";
import { Token, TokenAmount } from "../entities";

export default ({ api, store }: Context) => ({
  async swapTokens(token0Quantity: TokenAmount, token0: Token, token1: Token) {
    // USERINPUT: "Submit"
  },
});
