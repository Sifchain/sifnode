const { chromium } = require("playwright");
const expect = require("expect");

// https://github.com/NodeFactoryIo/dappeteer/tree/master/src
const METAMASK_PATH = "./extensions/dmkamcknogkgcdfhhbddcghachkejeap/0.8.1_0";
const KEPLR_PATH = "./extensions/nkbihfbeogaeaoehlefnkodbefgpgknn/9.1.1_0";

describe("connect to page", () => {
  beforeAll(async () => {});
  afterAll(async () => {});
  beforeEach(async () => {});
  afterEach(async () => {});
  it("should open new tab", async () => {});
});
