{
  "name": "sifchain-monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "app:serve": "cd ./app && yarn serve",
    "chain:migrate": "cd ./chains/ethereum && yarn migrate",
    "chain:start": "ganache-cli",
    "core:run-test": "cd core && yarn test",
    "core:test": "concurrently -k -s first \"yarn chain:start\" \"yarn chain:migrate && yarn core:run-test\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  },
  "workspaces": {
    "packages": [
      "core",
      "app",
      "chains/etherium"
    ]
  }
}
