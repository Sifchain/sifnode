// Types relating to the bank cosmos module
// As we flesh out API calls within the interface we can specify this further

export type BankBalances = any;
export type BankSenderAndTxInfo = any;
export type StdTx = any;
