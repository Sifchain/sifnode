export function createToken(chainId, address, decimals, symbol, name) {
    return {
        chainId,
        address,
        decimals,
        symbol,
        name,
    };
}
//# sourceMappingURL=Token.js.map