// These have mostly come off the API VOs and we may or may not need them
// As we flesh out API calls within the interface we can specify this further

export type Delegation = any;
export type UnbondingDelegation = any;
export type Redelegation = any;
export type Validator = any;
export type StakingPool = any;
export type StakingParameters = any;
